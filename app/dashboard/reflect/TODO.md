# Reflect TODOs

- Hook each section into the `TimeCapsule` model once the data layer is ready so entries auto-save.
- Provide an optional mentor sharing toggle without changing the default private setting, and log that preference.
- Add export/keepsake flows (PDF, prints, etc.) so memories can be preserved in other formats.
- Store voice notes securely and surface playback controls with clear privacy settings.
- Implement vault seal logic that locks entries and surfaces status for collaborators.
- Offer granular privacy controls per entry (share toggles, mentor visibility, etc.) for future releases.
